# Pinned environment for tools/export-laya-onnx.py. Install into tools/.venv (gitignored):
#
#   uv venv --python 3.12 tools/.venv
#   uv pip install --python tools/.venv/bin/python -r tools/requirements-laya.txt
#
# The reference implementation is the Laya authors' own package, installed from GitHub at a pinned
# commit, so the parity check compares ONNX against *their* forward pass, not a reimplementation.
laya @ git+https://github.com/NandhaKishorM/laya@c7527708f9f5220c669d8aa385077cd28d04708a
torch==2.8.0
transformers==5.0.0
safetensors==0.7.0
huggingface_hub==1.3.0
numpy==2.2.6
onnx==1.18.0
onnxscript==0.5.6
# Matches the Kotlin side (com.microsoft.onnxruntime:onnxruntime:1.20.0) as closely as PyPI allows:
# the parity numbers are only meaningful if Python and JVM run the same runtime generation.
onnxruntime==1.20.1
